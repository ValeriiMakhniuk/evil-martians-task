export enum ErrorMessages {
  UPPERCASE = "uppercase",
  LOWERCASE = "lowercase",
  ISNUMBER = "number",
  CONTAINS = "conatins",
}

export type InitialValues = {
  email: string;
  password: string;
};
