export enum ErrorMessages {
  UPPERCASE = "uppercase",
  LOWERCASE = "lowercase",
  ISNUMBER = "number",
  CONTAINS = "conatins",
  MINIMUM = "min",
}

export type TInitialValues = {
  email: string;
  password: string;
};
